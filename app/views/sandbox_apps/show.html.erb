<%= simple_form_for @app.feeds.first.events.new, url: events_path(@app.feeds.first) do |f| %>
  <%= f.input :data, label: "Input", as: "text" %>
  <%= f.button :submit, "Go" %>
<% end %>

<%= render "block_runs", block: @app.blocks.first %>
