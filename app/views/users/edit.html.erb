<h1>Edit profile</h1>

<%= simple_form_for(current_user, url: profile_path, method: :patch) do |f| %>
  <%= f.input :bio %>
  <%= f.input :location %>
  <%= f.input :website %>

  <%= f.button :submit, "Update Profile" %>
<% end %>
