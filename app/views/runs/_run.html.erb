<%= link_to(
  [run.user, run.block, run],
  class: "run-summary run-status-#{run.status}",
) do %>
  <%= run.status.to_s.titleize %>,
  <%= distance_of_time_in_words(Time.current, run.created_at) %> ago
<% end %>
