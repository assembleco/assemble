<h1><%= user.username %>'s blocks</h1>

<ul>
  <% @blocks.all.each do |block| %>
    <li>
      <%= link_to [block.user, block] do %>
        <%= block.name %>
      <% end %>
    </li>
  <% end %>
</ul>

<%= link_to "+ Create a new block", [:new, user, :block] %>
