<%= link_to(
  block_run_path(block_run.app.user, block_run.app, block_run),
  class: "block-run-summary block-run-status-#{block_run.status}",
) do %>
  <%= block_run.status.to_s.titleize %>,
  <%= distance_of_time_in_words(Time.current, block_run.created_at) %> ago
<% end %>
