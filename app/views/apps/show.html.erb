<div class="heading">
  <%= render "author_info", item: @app %>
  <h1><%= @app.name %></h1>
</div>

<div class="app-description">
  <%= simple_format(@app.description) %>
</div>

<%= link_to "Edit app", edit_app_path(@app.user, @app) %>
