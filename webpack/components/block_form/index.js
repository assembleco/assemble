import React from "react"
import $ from "jquery"

class BlockForm extends React.Component {
  render() {
    return (
      <div>Placeholder: Create a new block</div>
    );
  }
}

BlockForm.propTypes = {
}

export default BlockForm;
